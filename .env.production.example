# Nova8-managed: backend secrets this app expects at runtime.
# Copy this file to .env.production on your backend host and fill in
# real values. NEVER commit the filled-in file.
# (Values are intentionally omitted — they live in the Nova8 dashboard.)

OPENAI_API_KEY=
