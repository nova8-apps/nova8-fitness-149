// ─── Legacy schema stub ─────────────────────────────────────────────────────
// The app no longer uses Drizzle. All schema is declared as raw CREATE TABLE
// IF NOT EXISTS statements inside server/db.ts. This file is kept as a no-op
// so any old import paths resolve cleanly during the migration.
export {};
